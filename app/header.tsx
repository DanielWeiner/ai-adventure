'use client';

export default function Header({ userName, userImage, userEmail, loggedIn } : { userName: string, userImage: string, userEmail: string, loggedIn: boolean }) {
    return (
        <nav/>
    );
}